### Web Components

* Custom Elements
  ```html
  <new-element></new-element>
  ```
* Shadow DOM
  * `slot` and scoping CSS
* HTML Imports
  * Reusing your elements
* HTML Template
  ```html
  <template></template>
  ```